# ---- API ------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-multipart>=0.0.12

# ---- database -------------------------------------------------------------
SQLAlchemy>=2.0.35
alembic>=1.13
psycopg[binary]>=3.2

# ---- security -------------------------------------------------------------
passlib[argon2]>=1.7.4
argon2-cffi>=23.1
slowapi>=0.1.9

# ---- matching engine: THE PRODUCTION STACK -------------------------------
# These are not optional in production. MATCHING_STRICT=true makes the
# application refuse to start if any of them is missing, rather than scoring
# with a fallback while claiming this stack.
sentence-transformers>=3.0        # all-MiniLM-L6-v2, 384-dim
torch>=2.2                        # sentence-transformers runtime
faiss-cpu>=1.8                    # IndexFlatIP exact inner-product search
rapidfuzz>=3.9                    # token_set_ratio
scikit-learn>=1.5
numpy>=1.26
pandas>=2.2

# ---- LLM attribute extraction (structured parsing only) -------------------
httpx>=0.27                       # Ollama client; no vendor SDK needed

# ---- observability --------------------------------------------------------
structlog>=24.4
PyYAML>=6.0

# ---- tests ----------------------------------------------------------------
pytest>=8.3
pytest-asyncio>=0.24
